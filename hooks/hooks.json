{
  "description": "Nudge agents to use the 1nitetent skill for 1nt commands",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/nudge-skill.sh"
          }
        ]
      }
    ]
  }
}
